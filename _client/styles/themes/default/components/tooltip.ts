import TooltipThemeFactory from '@client/styles/themes/components/tooltip';

const tooltipTheme: TooltipThemeFactory = theme => ({
  defaultBackgroundColor: '#ffffff',
  defaultColor: '#000000',
});

export default tooltipTheme;
